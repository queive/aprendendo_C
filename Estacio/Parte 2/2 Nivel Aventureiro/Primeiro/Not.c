#include <stdio.h>

    int main(){
        int a;
        if (!(a > 0)) {
            printf("A variavel A é Negativa.\n");
        } else {
            printf("A variavel A é Positiva\n");
        }

        return 0;
    }